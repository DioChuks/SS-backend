# Allow illustrative placeholders in docs (real secrets must never be committed).
title = "StellarSettle API"

[allowlist]
description = "Documentation env examples are not real credentials"
paths = [
  '''(?i)readme\.md''',
  '''\.github\/PULL_REQUEST_TEMPLATE\.md''',
  '''CONTRIBUTING\.md''',
]
